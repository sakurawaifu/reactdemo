const codePointIsValid = (codePoint: number | string) => {
  if (typeof codePoint === 'string') {
    // codePoint todo
  }
}

const UnicodeUtils = {
  codePointIsValid
}

export default UnicodeUtils
